<?xml version="1.0" encoding="utf-8"?>
<!-- Small metadata badge (episode / duration / source) under the title. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/hikari_glass" />
    <stroke android:width="1dp" android:color="@color/hikari_glass_stroke" />
    <corners android:radius="11dp" />
</shape>
